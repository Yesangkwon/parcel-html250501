### UI 구현과 화면 구현
1. 폴더 관리하는 방법
  - 기본적인 정보 테스트하기
  - 브라우저를 통해서 테스트(html, css, js)를 할 때는 live server사용
  - 만일 js만 단독으로 테스트 할 때는 node 이름.js엔터하기(출력결과는 터미널 나감)
  - js만 집중적으로 확인하고자 할 때는 매번 node 이름.js하는 것은 불편함
  - nodemon : npm install -g nodemon
2. npm init으로 폴더 관리하는 방법
3. parcel 번들러 활용해서 폴더 관리하는 방법


### 수업소스 깃(깃허브)에 관리하기
1. 로컬레포를 먼저 작성해서 원격레포에 동기화 하기
  - 로컬 레포 -> 원격레포
  - 원격 레포 -> 로컬레포

2. 실습하기
```sh
  # 로컬 레포 -> 원격레포 
  # .git숨김 폴더 생성
  git init

  #
  git add .
  #
  git commit -m "수업실습 폴더 생성"

  # 깃허브에서 New Repository 생성하기
  # 주의사항 : 절대로 원격레포에 commit 발생하지 않도록 함

  git remote add origin 원격레포주소

  git push origin main
```

### 원격 레포 -> 로컬 레포
```sh
  # github에서 New Repository 버튼을 클릭하여 새로운 레포생성하기
  # 원격 레포에서 먼저 작업하므로 READEME.md 파일이 충돌 나지 않는다.
  # README.md추가하고 또 .gitignore 추가하기
  
  # 로컬에서 프로젝트 폴더 이름을 생성하기
  # VSCode에서 File > New Window 클릭한다.
  # 새 창에서 네번째 메뉴아이템에서 Clone Git Repository 클릭한다.
  # 상단에 열린 URL에 깃에서 생성한 레포 URL을 붙여넣는다.
```